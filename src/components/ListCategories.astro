---
import { getCategories } from '@/utils'
import Category from '@/components/Category'
const categories = await getCategories()

const { activeCategory } = Astro.props
---

<div class='relative flex flex-wrap min-w-full gap-5'>
	<Category name='Home' activeCategory={activeCategory} />
	<Category name='All Posts' activeCategory={activeCategory} />
	{
		categories.map((category: string) => (
			<Category name={category} activeCategory={activeCategory} />
		))
	}

	<div class='absolute w-full bottom-0 border-b-2 -z-40 dark:border-gray-600'></div>
</div>
